name: TimerHandler
version: ${project.version}
main: ${project.groupId}.${project.artifactId}
description: Handle the messages for the time left for the player
authors: [By_Jack]
minecraft-version: any